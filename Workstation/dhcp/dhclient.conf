# Check https://calomel.org/dhclient.html

option      rfc3442-classless-static-routes code 121 = array of unsigned integer 8;

send        host-name = gethostname();

request     subnet-mask, broadcast-address, time-offset, routers,
	        domain-name, domain-name-servers, domain-search, host-name,
	        dhcp6.name-servers, dhcp6.domain-search, dhcp6.fqdn, dhcp6.sntp-servers,
	        netbios-name-servers, netbios-scope, interface-mtu,
	        rfc3442-classless-static-routes, ntp-servers;

# If you want to block the DNS Servers to use only google
supersede   domain-name-servers 8.8.8.8,8.8.4.4;

# The timeout statement determines the amount of time that must pass between the time that the client
# begins to try to determine its address and the time that it decides that it's not going to be able
# to contact a server.
timeout 300;

